对robin这个“框架”的一些想法：

实际上没想做一个框架，因为成熟的框架太多了，而且laravel实在是让人爱不释手。

但在实际工作中，又有一些特殊的需求，要求很快完成一个项目。

这就有了robin的诞生，我把它称为“workspace”

robin更像一个小型的类库，同时又具有一些很奇怪的特性。

这些特性可能是我的一些特殊想法带来的：

1、自带后台，每一个数据表都必须声明它的结构，一旦声明，系统会更具这些声明来创建表，同时建立相应的管理后台。

2、带有控制器功能的视图层
